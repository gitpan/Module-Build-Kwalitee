0.15
  o symlink shipped tests to t/ so they are run for us too
  o use 'traditional' style of generating Makefile.PL

0.14
  o Improve description a bit

0.13
  o The stub M::B::K that ships with distributions now merely
    recommend some modules; if they are missing, some tests are
    skipped.  they do not exist. For module authors, however,
    these modules are all mandatory (so all tests will run).

  o for .pod files, do syntax checking but skip coverage checks

0.12 
  o Added Changes file

0.11
  o Minor pod fixes

0.10
  o Initial release to CPAN
